# Copia este fichero a prisma/.env para desarrollo local.
# Prisma CLI lo carga automáticamente — no hace falta exportar la variable.
# En producción DATABASE_URL la inyecta docker-compose desde el .env raíz.
DATABASE_URL="postgresql://vicalba:dev-password@localhost:5432/vicalba"
